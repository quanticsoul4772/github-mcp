# GitHub Personal Access Token (Required)
# Create at: https://github.com/settings/tokens
# Required scopes: repo, workflow, user, notifications (add more as needed)
GITHUB_PERSONAL_ACCESS_TOKEN=

# Alternative token variable (optional, GITHUB_PERSONAL_ACCESS_TOKEN takes precedence)
# GITHUB_TOKEN=

# GitHub API endpoint (optional, for GitHub Enterprise)
# GITHUB_HOST=https://github.enterprise.com/api/v3
# GITHUB_API_URL=https://github.enterprise.com/api/v3

# Read-only mode (optional)
# Set to 1 or true to prevent any write operations
# GITHUB_READ_ONLY=false

# Enabled toolsets (optional)
# Comma-separated list of toolsets to enable
# Available: context, repos, issues, pull_requests, actions, code_security, 
#           users, orgs, notifications, discussions, dependabot, secret_protection
# Default: all toolsets enabled
# Use "all" to enable everything
# GITHUB_TOOLSETS=repos,issues,pull_requests

# Dynamic toolset discovery (optional)
# Set to 1 or true to enable dynamic toolset loading
# GITHUB_DYNAMIC_TOOLSETS=false

# Node.js memory settings (optional, but recommended for large operations)
NODE_OPTIONS=--max-old-space-size=4096 --expose-gc --max-semi-space-size=64
